{
  "name": "http-request-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server server/db.json --watch --port 3001",
    "test": "vitest"
  },
  "devDependencies": {
    "json-server": "^0.17.4",
    "vite": "^6.2.0"
  }
}
